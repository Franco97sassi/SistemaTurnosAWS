# Frontend

Interfaz React + Vite del Sistema de Turnos. Consulta el [README principal](../../README.md) para conocer la arquitectura, configuración y despliegue.

```bash
cp .env.example .env
npm ci
npm run dev
```
